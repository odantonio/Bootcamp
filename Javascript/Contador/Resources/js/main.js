//var currentNumberWrapper = document.getElementById('currentNumber');
var currentNumber = 0;
document.getElementById("sobe").addEventListener("click", increment { document.getElementById("currentNumber").innerHTML = currentNumber + 1 });
document.getElementById("desce").addEventListener("click", decrement { document.getElementById("currentNumber").innerHTML = currentNumber - 1 });


/* function increment() {
    currentNumber = currentNumber + 1;
    currentNumberWrapper.innerHTML = currentNumber;
} */

/* function decrement() {
    currentNumber = currentNumber - 1;
    currentNumberWrapper.innerHTML = currentNumber;
} */
